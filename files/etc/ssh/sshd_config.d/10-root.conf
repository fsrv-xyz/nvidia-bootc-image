# Root login via SSH key only (no password). Keys are read from a path in the
# read-only /usr tree so login works with a transient root filesystem.
PermitRootLogin prohibit-password
AuthorizedKeysFile /usr/share/sshkeys/%u.keys
